
ファイルの場所: `/etc/systemd/system/slack-auto-invite.service`

```
# 停止
sudo systemctl stop slack-auto-invite

# 状態
sudo systemctl status slack-auto-invite

# 有効化
sudo systemctl enable --now slack-auto-invite
```
